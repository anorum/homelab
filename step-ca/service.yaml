apiVersion: v1
kind: Service
metadata:
  name: step-ca
spec:
  type: LoadBalancer
  allocateLoadBalancerNodePorts: false
  selector:
    app: step-ca
  ports:
    - name: https
      port: 443
      targetPort: https
      protocol: TCP
