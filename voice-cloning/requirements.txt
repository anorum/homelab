# Core audio processing
openai-whisper       # Speech-to-text for transcribing recordings
pydub                # Audio manipulation
soundfile            # WAV file I/O
numpy                # Audio array processing

# XTTS v2 voice cloning (Coqui TTS)
TTS                  # pip install TTS — includes XTTS v2

# ElevenLabs (optional — cloud voice cloning)
elevenlabs           # pip install elevenlabs

# Piper training (install separately — see README)
# git clone https://github.com/rhasspy/piper.git /opt/piper
# cd /opt/piper/src/python && pip install -e .
# pip install onnx onnxruntime

# Monitoring training
tensorboard
