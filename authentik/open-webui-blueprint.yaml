apiVersion: v1
kind: ConfigMap
metadata:
  name: open-webui-blueprint
  namespace: authentik
  labels:
    blueprints.goauthentik.io/instantiate: "true"
data:
  open-webui.yaml: |
    version: 1
    metadata:
      name: Open WebUI OAuth2 Provider
      labels:
        blueprints.goauthentik.io/instantiate: "true"
    context:
      client_id: open-webui
      client_secret: !Env OPEN_WEBUI_OAUTH_CLIENT_SECRET
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: open-webui-provider
        state: present
        identifiers:
          name: Open WebUI
        attrs:
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Internal JWT Certificate]]
          client_id: !Context client_id
          client_secret: !Context client_secret
          client_type: confidential
          redirect_uris:
            - url: https://chat.home.alexnorum.com/oauth/oidc/callback
              matching_mode: strict
          include_claims_in_id_token: true
          sub_mode: hashed_user_id
          issuer_mode: per_provider
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]

      - model: authentik_core.application
        state: present
        identifiers:
          slug: open-webui
        attrs:
          name: Open WebUI
          provider: !KeyOf open-webui-provider
          meta_launch_url: https://chat.home.alexnorum.com
          policy_engine_mode: any
