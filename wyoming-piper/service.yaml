apiVersion: v1
kind: Service
metadata:
  name: wyoming-piper
  namespace: wyoming-piper
spec:
  selector:
    app: wyoming-piper
  ports:
    - port: 10200
      targetPort: 10200
      protocol: TCP
