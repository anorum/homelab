apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: backup

resources:
  - namespace.yaml
  - rbac.yaml
  - backup-script-configmap.yaml
  - cronjob.yaml

generators:
  - secret-generator.yaml
