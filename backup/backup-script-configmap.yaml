apiVersion: v1
kind: ConfigMap
metadata:
  name: backup-script
  namespace: backup
data:
  backup.sh: |
    #!/bin/sh
    set -euo pipefail

    DATE=$(date +%Y-%m-%d)
    BUCKET="anorum-homelab"
    BACKUP_PREFIX="backups"

    echo "=== Homelab Backup - $DATE ==="

    # --- Mealie backup ---
    echo "Backing up Mealie data..."
    aws s3 sync /mnt/hd/mealie/ "s3://$BUCKET/$BACKUP_PREFIX/mealie/" \
      --delete \
      --region "$AWS_DEFAULT_REGION"
    echo "Mealie backup complete."

    # --- Authentik PostgreSQL backup ---
    echo "Backing up Authentik database..."
    kubectl exec -n authentik authentik-postgresql-0 -- \
      sh -c 'PGPASSWORD="$(cat /opt/bitnami/postgresql/secrets/postgresql-password)" pg_dump -U authentik -d authentik' \
      | aws s3 cp - "s3://$BUCKET/$BACKUP_PREFIX/authentik/$DATE.sql" \
        --region "$AWS_DEFAULT_REGION"
    echo "Authentik backup complete."

    # --- Cleanup old backups (keep 7 days) ---
    echo "Cleaning up old backups..."
    CUTOFF_DATE=$(date -d @$(($(date +%s) - 604800)) +%Y-%m-%d)
    # open-webui was dropped from this list when the service was retired. Its
    # existing snapshots under $BACKUP_PREFIX/open-webui/ are deliberately left
    # in S3 rather than aged out: they are the only surviving copy of that
    # database once the PVC is gone. Delete the prefix by hand when sure.
    for prefix in authentik; do
      aws s3 ls "s3://$BUCKET/$BACKUP_PREFIX/$prefix/" --region "$AWS_DEFAULT_REGION" \
        | awk '{print $4}' \
        | while read -r file; do
            file_date=$(echo "$file" | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}')
            if [ -n "$file_date" ] && [ "$file_date" \< "$CUTOFF_DATE" ]; then
              echo "Deleting old backup: $prefix/$file"
              aws s3 rm "s3://$BUCKET/$BACKUP_PREFIX/$prefix/$file" --region "$AWS_DEFAULT_REGION"
            fi
          done
    done
    echo "Cleanup complete."

    echo "=== All backups completed successfully ==="
