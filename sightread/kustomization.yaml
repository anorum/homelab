apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

commonLabels:
  app.kubernetes.io/name: sightread
  app.kubernetes.io/instance: sightread

namespace: sightread
